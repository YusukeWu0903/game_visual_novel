{
  "name": "web-visual-novel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python ../scripts/csv2json.py && vite",
    "build": "python ../scripts/csv2json.py && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  }
}
